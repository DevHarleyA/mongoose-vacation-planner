<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <title>Vacation Destinations WishList</title>
</head>

<body>
    
    <header class="text-center bg-light pt-5 pb-5">
        <h1 class="display-3">Vacation Destination WishList</h1>
        <p class="fw-light">Found exciting places to visit on your next vacation? Use the form below to add them to your wishlist</p>
    </header>

    <div class="container">
        <div class="row">

            <div class="col-md-3 form-area w-25 pt-4">
                <form action="/destinations" method="POST">
                    <div class="mb-3">
                        <label for="destinationName">Destination Name</label><br>
                        <input type="text" name="destinationName" id="destinationName" class="form-control" placeholder="Enter the name of the destination" required minlength="2">
                    </div>
                    <div class="mb-3">
                        <label for="location">Location</label><br>
                        <input type="text" name="location" id="location" class="form-control" placeholder="Where is it located?" required minlength="2">
                    </div>
                    <div class="mb-3">
                        <label for="description">Description</label><br>
                        <textarea type="text" name="description" id="description" class="form-control" placeholder="Why do you want to go there?"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary mb-3 form-control submitButton">Add to list</button>
                </form>
            </div>

            <div class="col-md-9 pt-4">
                <% if (vacay.length === 0) {%>
                <p class="fw-bold fs-4 wishList text-center">Enter Destination Details</p>
                <% } else {%>
                <div class="row row-cols-1 row-cols-md-3 g-4 vacays">
                    <% for (let i = 0; i < vacay.length; i++) {%>
                        <div class="col">
                            <div class="card" style="width: 18rem;">
                                <% if (vacay[i].imgSRC === '') {%>
                                <img class="card-img-top" src="img/default_pic.jpeg" alt="">
                                <% } else {%>
                                <img class="card-img-top" src="<%= vacay[i].imgSRC%>" alt="">
                                <% } %>
                                <div class="card-body">
                                    <h4 class="card-title"><%= vacay[i].location%></h4>
                                    <h6 class="card-title"><%= vacay[i].destinationName%></h6>
                                    <p class="card-text"><%= vacay[i].description%></p>
                                    <button class="btn btn-primary me-2 editBtn">Edit Place</button>
                                    <button class="btn btn-danger me-2 deleteBtn">Delete Place</button>
                                </div>
                            </div>
                        </div>
                    <% } %>
                </div>
                <% } %>
            </div>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>  
    <script src="/main.js"></script>
</body>

</html>